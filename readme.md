{
  first of all run npm insatll and then copy link from tailwind css and run in terminal,

  so then go in config.js copy tailwind line and plugin statement paste exact same place where needed.

  than copy import tailwind and paste on index.css
}

{
  const [color, setColor] = useState('blue'),
  here setColor is a method while only color is a parameter or callback for it.

  that is valid: (() => setColor{('color')}
  )
}

{
hooks = useState, useCallbacks, useEffects, useRef
}

all the dependicies where which one is needed and how to give them exact or matching need or logic whatever they liked to take(useState, useEffect)

--javaScript har bar event ma cheezon ko str form ma leti hai thats why use karo Number()

remember the key in the react, bcoz may be it becomes the reason of performaces degrader

link = a -> not use a tag bcoz its lead to rerendering entrie page

NavLink = additional things-> href = to